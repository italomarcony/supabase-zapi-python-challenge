# Supabase Z-API Python Challenge

Projeto em Python que lê contatos do Supabase e envia mensagens personalizadas via Z-API.

## Objetivo

Buscar até 3 contatos do banco Supabase e enviar a mensagem:

```text
Olá, <nome_contato> tudo bem com você?
```

## Estrutura do projeto

```text
.
├── main.py
├── services/
│   ├── supabase_client.py
│   └── zapi_client.py
├── utils/
│   ├── logger.py
│   └── phone.py
├── .env.example
├── .gitignore
├── README.md
└── requirements.txt
```

## Setup da tabela no Supabase

Criar a tabela `contacts` com os campos:

- `id` (gerado automaticamente)
- `name` (text)
- `phone` (text)

Exemplo de SQL:

```sql
create table if not exists public.contacts (
    id bigint generated by default as identity primary key,
    name text not null,
    phone text not null,
    created_at timestamp with time zone default now()
);
```

Caso use RLS, habilite leitura para `anon`:

```sql
create policy "Allow anon read contacts"
on public.contacts
for select
to anon
using (true);
```

## Variáveis de ambiente

1. Crie um arquivo `.env` na raiz do projeto.
2. Use o arquivo `.env.example` como base.
3. Preencha as variáveis abaixo com os seus valores reais:

```env
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_TABLE=contacts

ZAPI_BASE_URL=https://api.z-api.io
ZAPI_INSTANCE_ID=
ZAPI_TOKEN=
ZAPI_CLIENT_TOKEN=
```

## Como rodar o projeto

### 1. Clonar o repositório

```bash
git clone 
cd supabase-zapi-python-challenge
```

### 2. Criar o ambiente virtual

```bash
python -m venv .venv
```

### 3. Ativar o ambiente virtual

#### Windows (PowerShell)

```powershell
.venv\Scripts\Activate.ps1
```

#### Windows (cmd)

```cmd
.venv\Scripts\activate
```

#### Linux / macOS / Git Bash

```bash
source .venv/bin/activate
```

### 4. Instalar as dependências do projeto

O projeto usa o arquivo `requirements.txt` para instalar as bibliotecas necessárias.

```bash
pip install -r requirements.txt
```

### 5. Conferir se o arquivo `.env` foi criado

Antes de executar, confirme que existe um arquivo `.env` na raiz do projeto com as credenciais do Supabase e da Z-API.

### 6. Executar o projeto

```bash
python main.py
```

## Regras implementadas

- Leitura dos contatos a partir do Supabase
- Limite de até 3 contatos
- Normalização simples de telefone
- Mensagem personalizada com o nome do contato
- Uso de `.env`
- Logs simples no terminal
- Tratamento básico de erros

## Exemplo de mensagem enviada

```text
Olá, Ana Silva tudo bem com você?
```